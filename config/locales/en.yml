# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  time:
    input:
      formats: "[:slashes]"
    formats:
      slashes: "%d/%m/%Y"
    embed:
      slashes: "dd/mm/yyyy"

  helpers:
    submit:
      create: "Create %{model}"
      update: "Update %{model}"

  activerecord:
    models:
      visit: "Booking"

    attributes:
      visit:
        client_name: "Client"
        car_name: "Car"
        description: "Details"
        phone_number: "Phone"
        color: "Color"

    errors:
      models:
        visit:
          inconsistent_workshop: "Form data is inconsistent"
          date_range_invalid: "Date range is invalid"

  visits:
    form:
      booking_info: "Booking info"
      range_to: "to"

    order_fields:
      price_vat_html: "Price (VAT)"
      price_total_html: "Price"
      add_service_html: "Add service"
      total_time_html: "Total time"
      total_price_html: "Total price"
<%= form_for([@visit, @visit.contacts.build]) do |f| %>
    <p>
      <%= f.label :value %><br>
      <%= f.text_field :value %>
    </p>
    <p>
      <%= f.label :info %><br>
      <%= f.text_area :info %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>